% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utilities.R
\name{write_sheets}
\alias{write_sheets}
\title{Write Multiple Dataframes to Google Sheets or Local Excel File}
\usage{
write_sheets(
  .data,
  sheet_names,
  name,
  local = FALSE,
  download = FALSE,
  path = NULL
)
}
\arguments{
\item{.data}{A list of dataframes to save}

\item{sheet_names}{A character vector of sheet names (must match length of \code{.data})}

\item{name}{A string specifying the name of the spreadsheet/file to create}

\item{local}{Logical. Whether to save directly to a local Excel file. Default is FALSE.}

\item{download}{Logical. Whether to download the Google spreadsheet as an Excel file. Default is FALSE.}

\item{path}{Optional. Path where the Excel file will be saved.
If NULL (default), saves to working directory with \code{name}.}
}
\value{
A tibble with:
\itemize{
\item spreadsheet_id: The ID of the created Google spreadsheet (if local=FALSE)
\item spreadsheet_url: The URL of the created Google spreadsheet (if local=FALSE)
\item file_path: The local path where the Excel file was saved (if local=TRUE or download=TRUE)
}
}
\description{
Takes multiple dataframes and writes them to separate sheets in either:
\enumerate{
\item A new Google Spreadsheet (default)
\item A local Excel file (when local = TRUE)
}
}
\details{
When using Google Sheets (option 1), you can optionally download
the spreadsheet as an Excel file (when download = TRUE)
}
\examples{
\dontrun{
library(dplyr)

# Create sample dataframes
student_data <- tibble(
  id = 1:5,
  name = c("John", "Emma", "David", "Sarah", "Michael"),
  score = c(85, 92, 78, 95, 88)
)

product_data <- tibble(
  product_id = 101:105,
  product_name = c("Laptop", "Smartphone", "Tablet", "Earphones", "Mouse"),
  price = c(850, 950, 600, 150, 50)
)

# Basic usage - write to Google Sheets
list(student_data, product_data) \%>\%
  write_sheets(
    sheet_names = c("Students", "Products"),
    name = "Sample_Data"
  )

# Write to local Excel file
list(student_data, product_data) \%>\%
  write_sheets(
    sheet_names = c("Students", "Products"),
    name = "Sample_Data",
    local = TRUE
  )

# Write to Google Sheets and download
result <- list(student_data, product_data) \%>\%
  write_sheets(
    sheet_names = c("Students", "Products"),
    name = "Sample_Data",
    download = TRUE
  )

# Access the spreadsheet URL
result$spreadsheet_url
}

}
