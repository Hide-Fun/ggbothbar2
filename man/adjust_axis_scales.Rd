% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/align_scales.R
\name{adjust_axis_scales}
\alias{adjust_axis_scales}
\title{Adjust axis scales on one ggplot object}
\usage{
adjust_axis_scales(
  plot,
  x_break_step = 3,
  y_break_step = 3,
  x_limits = NULL,
  y_limits = NULL,
  aspect_ratio = NULL,
  clip = "off",
  expand_breaks = FALSE,
  expand = ggplot2::waiver()
)
}
\arguments{
\item{plot}{A ggplot object.}

\item{x_break_step, y_break_step}{Positive finite break spacing in the
original data space.}

\item{x_limits, y_limits}{Optional visible limits. Supply one finite number or
\code{c(min, max)} in the original data space.}

\item{aspect_ratio}{Optional positive finite aspect-ratio multiplier.}

\item{clip}{Clipping mode passed to the coordinate system: \code{"on"}, \code{"off"},
or \code{"inherit"}.}

\item{expand_breaks}{If \code{TRUE}, extend each upper limit until its span is an
integer multiple of the corresponding break step.}

\item{expand}{Expansion specification applied to adjusted continuous scales.}
}
\value{
An adjusted ggplot object.
}
\description{
\code{adjust_axis_scales()} changes visible ranges, break spacing, expansion, and
an optional aspect ratio while preserving supported scale transformations
and coordinate systems. Continuous log scales and \code{\link[ggplot2:coord_flip]{ggplot2::coord_flip()}}
are preserved.
}
\details{
Free-scale facets and unsupported coordinates return a warned best-effort
fallback. The warning has class \code{ggbothbar_axis_fallback_warning}.
}
\examples{
library(ggplot2)
plot <- ggplot(mtcars, aes(wt, mpg)) + geom_point()
adjust_axis_scales(
  plot,
  x_break_step = 0.5,
  y_break_step = 5
)
}
