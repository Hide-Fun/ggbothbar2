% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/axis-scales.R
\name{align_axis_scales}
\alias{align_axis_scales}
\title{Align axis scales across ggplot objects}
\usage{
align_axis_scales(
  plots,
  axes = c("x", "y"),
  x_break_step = 3,
  y_break_step = 3,
  x_limits = NULL,
  y_limits = NULL,
  aspect_ratio = NULL,
  clip = "off",
  expand_breaks = FALSE,
  expand = ggplot2::waiver()
)
}
\arguments{
\item{plots}{A list containing at least two ggplot objects.}

\item{axes}{Axes to align: \code{"x"}, \code{"y"}, or \code{c("x", "y")}.}

\item{x_break_step, y_break_step}{Positive finite break spacing in the
original data space.}

\item{x_limits, y_limits}{Optional visible limits. Supply one finite number or
\code{c(min, max)} in the original data space.}

\item{aspect_ratio}{Optional positive finite aspect-ratio multiplier.}

\item{clip}{Clipping mode passed to the coordinate system: \code{"on"}, \code{"off"},
or \code{"inherit"}.}

\item{expand_breaks}{If \code{TRUE}, extend each upper limit until its span is an
integer multiple of the corresponding break step.}

\item{expand}{Expansion specification applied to adjusted continuous scales.}
}
\value{
A list of adjusted ggplot objects in input order.
}
\description{
\code{align_axis_scales()} gives multiple plots common visible ranges and break
positions while retaining each plot's supported scale transformation and
coordinate semantics. Continuous log scales and \code{\link[ggplot2:coord_flip]{ggplot2::coord_flip()}} are
preserved.
}
\details{
Free-scale facets and unsupported coordinate systems use a documented
best-effort fallback. The function emits a structured
\code{ggbothbar_axis_fallback_warning} before returning those plots because a
fallback may change facet or coordinate meaning.
}
\examples{
library(ggplot2)
first <- ggplot(subset(mtcars, cyl == 4), aes(wt, mpg)) + geom_point()
second <- ggplot(subset(mtcars, cyl == 6), aes(wt, mpg)) + geom_point()

aligned <- align_axis_scales(
  list(first, second),
  axes = c("x", "y"),
  x_break_step = 0.5,
  y_break_step = 5
)
}
